// Placeholder shell. The real routed AppShell (Projects Index, Peek, full-page Overview)
// is built in Task 9; Task 8 only establishes the contract, query boundary, and tests, so
// this keeps the build graph valid without shipping any deferred UI.

export function App() {
  return (
    <main>
      <h1>OmniProj</h1>
    </main>
  );
}
