[package]
name = "omniproj-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "OmniProj internal: domain types + ~/.omniproj layout (hexagonal core; no async/net/LLM)."
publish = false

# Domain crate: types + ~/.omniproj layout. NO async / net / LLM deps (hexagonal core).
[dependencies]
fs2.workspace = true
serde.workspace = true
sha2.workspace = true
regex.workspace = true
dirs.workspace = true
toml.workspace = true
chrono.workspace = true
uuid.workspace = true

[dev-dependencies]
serde_json.workspace = true
